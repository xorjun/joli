# Server Log Book

Servers Environment Name : arjnet

Last updated: 2026-05-30

## 2026-05-30

- Created the Joli AI Career Concierge — a chat-first job application platform — as a new standalone service in this repository.
- Built the full backend in Python/FastAPI with async SQLAlchemy, JWT auth, 13 database models covering German job market specifics (UserProfile with Persönliche Daten, WorkExperience, Education, LanguageSkill with CEFR levels, TechSkill, Certificate, Zeugnis, ChatSession, ChatMessage, JobApplication, GeneratedDocument).
- Integrated OpenRouter AI with three specialized personas: chat coach (DeepSeek) for conversational profile building, document writer (Claude Sonnet) for DIN 5008 resume/cover letter generation, and Arbeitszeugnis decoder for German coded reference letters.
- Implemented DIN 5008–compliant DOCX generation (python-docx) for tabellarischer Lebenslauf and Anschreiben, with LibreOffice headless PDF conversion in the Docker image.
- Built job ad scraper targeting German boards (StepStone, Indeed.de, XING, Arbeitsagentur) with AI-powered requirements extraction.
- Implemented profile extraction engine that parses AI chat responses for structured profile updates, incrementally building user profiles through conversation.
- Built React frontend with glass-morphism UI (Tailwind CSS, Framer Motion), split-panel chat interface, JWT auth flow, German/English i18n, and auto-detection of job URLs in chat.
- Created Docker Compose dev setup (backend :8000, frontend :5173 proxy) and production compose for OCI deployment.
- Pushed initial commit to GitHub (xorjun/joli) with comprehensive codebase.
- Deployed Joli to OCI production at `/home/ubuntu/services/joli/`: built Docker image with LibreOffice, connected to existing `oci-ai-stack_postgres` using the `joli` database, bound to internal port `100.101.107.42:8085`.
- Added `joli-oci-https` router and `joli-oci` service to Hetzner Traefik file provider at `/data/coolify/proxy/dynamic/oci-ai-stack.yaml`, routing `joli.arjun.cloud` → `http://100.101.107.42:8085`.
- Fixed Traefik config rejection caused by `serversTransport` field in wrong YAML location under `drive-oci` service — moved it under `loadBalancer` where Traefik v3 expects it; after the fix, all routes including joli loaded successfully.
- Validated Joli end-to-end: HTTPS at `https://joli.arjun.cloud` returns 200 on `/health`, serves React SPA on `/`, and API auth on `/api/auth/register` returns valid JWT tokens.
- Created Docker Compose dev setup (backend :8000, frontend :5173 proxy) and production compose for OCI deployment.
- Pushed initial commit to GitHub (xorjun/joli) with 53 files and comprehensive codebase.
- TODO: Deploy to OCI server as `/home/ubuntu/services/joli/`, configure Traefik route for `joli.arjun.cloud` → OCI internal port, and validate end-to-end.
- TODO: Add `joli` DNS record in Cloudflare dashboard for `arjun.cloud` pointing to Hetzner origin (49.13.3.97) so Cloudflare proxies traffic to Hetzner. Without this, requests to joli.arjun.cloud are 404'd at Cloudflare edge before reaching Hetzner Traefik.

## 2026-05-30 (Joli OCI Deployment)

- Deployed Joli to OCI at `/home/ubuntu/services/joli/` on OCI server (92.5.35.107 / 100.101.107.42).
- Created `joli` database in existing OCI Postgres (`letta` user on `oci-ai-stack-postgres-1`).
- Built Docker image with LibreOffice for PDF conversion, connected to `oci-ai-stack_default` network.
- Joli backend running healthy on `100.101.107.42:8085`, serving both API and frontend SPA.
- Configured Hetzner Traefik (`oci-ai-stack.yaml`) with `joli-oci-https` router and `joli-oci` service pointing to `http://100.101.107.42:8085`.
- Internal validation: `/health` returns 200, frontend HTML serves, API auth works (register/login).
- External HTTPS blocked: Cloudflare DNS for `joli.arjun.cloud` resolves to Cloudflare proxy IPs but Cloudflare returns 404 before forwarding to Hetzner. A Cloudflare DNS record or origin rule for `joli` subdomain is needed.
- Cleaned up Traefik config: regenerated `oci-ai-stack.yaml` with proper YAML structure including all 9 existing services plus joli.

This document records the completed work currently represented in this repository for the two active servers in this stack.

Dates below are based on the most defensible local evidence available in this workspace:

- explicit dates captured in repo notes
- modification dates on the config files that define the current server state

If a change spanned multiple edits, it is logged on the nearest verifiable date.

Infrastructure in scope:

- Hetzner edge and control host, reachable on Tailscale at `100.64.75.39`
- OCI workload host, kept private behind Tailscale at `100.101.107.42`

## 2026-05-30

- Recorded the next ArjNet platform direction as a private AI operating system built on the services already live in this repo: Authentik for identity, LiteLLM for model routing and policy, Letta for stateful agents, Qdrant for retrieval, MCPO for tools, n8n plus the compute gateway for jobs, and OCI/Modal/Kaggle/Colab for execution.
- Captured the intended platform spine for the next phase before additional infrastructure changes: add a central ArjNet API plus database layer that owns workspaces, users, roles, prompt versions, agents, tool bindings, jobs, datasets, artifacts, approvals, and evaluations instead of exposing each backend directly.
- Locked the recommended implementation order for this phase so future server work stays incremental instead of turning into a broad platform rewrite: `workspace and registry layer -> unified ArjNet API -> job ledger and scheduler -> prompt and agent registry -> knowledge ingestion pipeline -> approval and guardrail layer -> unified web app`.
- Defined the MVP behavior for the private AI OS so follow-on deployments can be judged against a stable target: Authentik-backed workspace login, approved agent chat, knowledge-base attachment and retrieval, tracked async jobs, prompt and model version comparison, approval gates for sensitive actions, and an operator-facing audit feed for model/tool usage.
- Scoped the first high-value implementation surfaces to the current infra model rather than adding new platforms prematurely: keep OCI as the always-on serving and control plane, keep Modal as the automated burst executor, keep Kaggle for queued batch notebooks, keep Colab interactive-only, and route all sanctioned model access through LiteLLM with workspace-aware policy and cost controls.
- Explicitly deferred non-essential platform expansion for this phase so the next server changes stay focused on the control plane: no Kubernetes rebuild, no feature store, no distributed training scheduler, no multi-region serving mesh, and no plugin marketplace until the ArjNet registry, job tracking, and approval layers exist.
- Implemented the first repo-side ArjNet control-plane slice in `oci-ai-stack/arjnet-ui.py`: the existing FastAPI compute service now also initializes a Postgres-backed `arjnet` schema on startup with registry tables for `workspaces`, `prompt_versions`, `agent_registrations`, `tool_bindings`, `job_runs`, `artifacts`, and `approvals`.
- Added a new control-plane API surface under `/api/v1/control/*` for registry and ledger operations, including overview, workspace, prompt, agent, tool, job, artifact, and approval endpoints, so the service can act as the first unified ArjNet API layer instead of only wrapping compute submissions.
- Added bootstrap behavior for the control plane so the repo-side service auto-creates the `platform` workspace by default and can auto-record Modal, Kaggle, and Colab submit/handoff calls from the existing compute endpoints into the new `job_runs` ledger.
- Wired the new control-plane slice into the OCI stack definition by making `arjnet-ui` depend on Postgres health, installing `psycopg[binary]` in `arjnet-ui.Dockerfile`, and supplying safe default database/bootstrap settings in `oci-ai-stack/docker-compose.yml` so the control-plane registry can come up without a separate database service or an expanded operator checklist.
- Documented the new override knobs in `oci-ai-stack/.env.example` for database schema, bootstrap workspace, and automatic compute-job recording while keeping the default runtime path anchored to the existing OCI Postgres service.
- Validated the repo-side control-plane implementation with `python -m py_compile oci-ai-stack/arjnet-ui.py`, `docker compose -f oci-ai-stack/docker-compose.yml config`, and workspace diagnostics on the touched files; no file-level errors remained after the final indentation and default-env fixes.
- Rolled the first control-plane slice live to OCI by backing up the existing `arjnet-ui.py`, `arjnet-ui.Dockerfile`, and `docker-compose.yml` files in `/home/ubuntu/services/oci-ai-stack/`, syncing the updated repo versions over SSH with the OCI key, rebuilding the `arjnet-ui` image, and recreating the `oci-ai-stack-arjnet-ui-1` container.
- Added the first real product surface for the private AI OS to the live `compute.arjun.cloud` dashboard: a new `Control Plane Registry` section now renders workspace, prompt, agent, approval, and job-ledger panels backed by `/api/v1/control/*` instead of limiting the UI to raw compute submit/poll forms.
- Extended the live control-plane behavior beyond initial submit logging: `/api/v1/poll` now mutates the matching `job_runs` row by `external_ref`, `/api/v1/register-result` now updates the matching row and records notebook output artifacts, and `/api/v1/control/jobs/{job_id}` plus `PATCH /api/v1/control/jobs/{job_id}` now expose direct job-status inspection and manual status mutation.
- Fixed the first live rollout bug in `arjnet-ui.py` after public validation: the FastAPI startup and shutdown hooks for control-plane bootstrap were present in the file but had been introduced inside the wrong indentation block during repo edits, so the running service could start while never creating the `arjnet` schema. After correcting the hook placement, syncing the file again, and explicitly restarting `arjnet-ui`, the OCI Postgres database showed the full `arjnet` schema with all seven registry tables.
- Validated the live rollout from the public edge with a browser-style user agent and direct OCI inspection: `https://compute.arjun.cloud/` returns the updated dashboard HTML with the `Control Plane Registry` section, `GET /api/v1/control/overview` returns the bootstrap workspace and registry counts, and the OCI Postgres container reports the live `arjnet.workspaces`, `prompt_versions`, `agent_registrations`, `tool_bindings`, `job_runs`, `artifacts`, and `approvals` tables.
- Validated the new live job-ledger mutation path end to end by submitting a real Modal `document_preprocess` job through `https://compute.arjun.cloud/api/v1/submit/modal/json`, polling it through `POST /api/v1/poll`, and then fetching the recorded row through `GET /api/v1/control/jobs/{job_id}`. The public result showed a stable `registry_job_id`, the polled job completed successfully, and the persisted ledger row reflected `status=completed`, `provider=modal`, `job_type=document_preprocess`, the real Modal `call_id`, and a non-empty `result_payload`.
- Extended the live control-plane schema and API with the first real workspace-role model for ArjNet: `identity_principals` plus `workspace_memberships` now live in the `arjnet` schema, `GET/POST /api/v1/control/identities` and `GET/POST /api/v1/control/memberships` are exposed publicly, and `create_workspace` now turns a supplied `owner_email` into an `owner` membership instead of keeping ownership as a detached string field.
- Added the next live UI slice for the private AI OS directly in `compute.arjun.cloud`: the control-plane section now includes `Authentik Session` and `Workspace Access` cards so operator-visible role mapping can happen from the same web surface as prompt, agent, approval, and job-ledger work.
- Added request-header normalization for Authentik forward-auth in `arjnet-ui.py` and exposed it through `GET/POST /api/v1/control/authentik/session`, so the control plane can inspect forwarded `X-authentik-*` identity headers and bind the current browser session into a workspace role once the compute host is placed behind Authentik at the edge.
- Rolled the Authentik role-model slice live by syncing only the updated `arjnet-ui.py` into `/home/ubuntu/services/oci-ai-stack/`, creating a timestamped backup of the previous live file first, and explicitly restarting `arjnet-ui` so the new control-plane tables were created by the startup hook.
- Validated the live role-model behavior from the public API by creating workspace `validation-role-model-1780155143` with `owner_email=validation-owner@arjnet.local`, confirming that `GET /api/v1/control/memberships?workspace_slug=validation-role-model-1780155143` returned an automatic `owner` membership, then adding a second `approver` mapping through `POST /api/v1/control/memberships` and confirming both identities appeared through `GET /api/v1/control/identities?workspace_slug=validation-role-model-1780155143`.
- Confirmed the live control overview now tracks the new access layer counts as well: before validation the public overview showed `principal_count=0` and `membership_count=0`; after the validation workspace and approver mapping it showed `principal_count=2` and `membership_count=2`.
- Added the missing Authentik proxy objects for `compute.arjun.cloud` itself by creating `Provider for ArjNet Compute Fabric` plus application slug `arjnet-compute` in the live Authentik admin API and then appending provider `17` to the embedded outpost `e44a64a6-c193-4974-bc83-da6f153acc01`; without that outpost update, protecting the compute host at Traefik would have redirected to Authentik but still failed after login.
- Completed the edge-side protection step by updating `hetzner-traefik/arjun-authentik-sso.yaml`, syncing it to `/data/coolify/proxy/dynamic/arjun-authentik-sso.yaml` on Hetzner, and attaching the existing `coolify-authentik` forward-auth middleware to `compute-auth-https` so the protected router now wins for `compute.arjun.cloud`.
- Validated the public edge after the Traefik rollout with an unauthenticated `curl -I https://compute.arjun.cloud`: the host now returns `302 Found` to Authentik's `/application/o/authorize/` endpoint instead of serving the compute UI directly, proving the browser surface is now SSO-gated.
- Validated the authenticated browser path end to end after the edge change: an Authentik-authenticated browser session successfully loaded `https://compute.arjun.cloud/`, `GET /api/v1/control/authentik/session` returned `authenticated=true` with forwarded `X-authentik-*` identity data for `mrdioxide46@gmail.com`, and `POST /api/v1/control/authentik/session` created a persisted `admin` membership on workspace `platform` for that live session principal.
- Added the first artifact-library and knowledge-ingestion surface directly to `compute.arjun.cloud`: the existing control-plane dashboard now has an `Artifact Library` card that posts to `/api/v1/ingest`, lists recent `/api/v1/control/artifacts` entries, and turns successful knowledge ingests into visible registry records instead of leaving them only in Qdrant and Letta.
- Extended `oci-ai-stack/arjnet-ui.py` so `POST /api/v1/ingest` now accepts a workspace context, validates the target workspace before submission, records successful ingests as `knowledge_document` artifacts with `qdrant://<collection>/<point_id>` URIs, and returns the created artifact metadata alongside the ingest result. The control-plane `artifacts` endpoint was also widened with a `kind` filter so the dashboard can load a focused knowledge-library view.
- While validating the new ingestion surface live, the first real ingest failed end to end because `oci-ai-stack/n8n-ai-stack-gateway.js` still generated raw SHA-256 hex strings for Qdrant `point_id` values. Qdrant accepted only unsigned integers or UUIDs on the live `ai_stack_documents` collection, so the gateway returned `HTTP 400` until `buildPointId` was changed to emit a deterministic UUID-form identifier derived from the hash seed.
- Rolled that gateway repair live by syncing the updated `n8n-ai-stack-gateway.js` into `/home/ubuntu/services/oci-ai-stack/`, creating a timestamped backup of the previous live file first, and explicitly restarting `ai-stack-gateway` so the bind-mounted code change was actually loaded in memory.
- Validated the repaired ingestion path at three levels after the gateway restart: direct OCI `POST http://ai-stack-gateway:8081/execute` succeeded for `ingest_document`, the public `https://n8n.arjun.cloud/webhook/ai-stack-ping-workflow/webhook/ai-stack-ping` route again returned a successful `ingest_document` result, and an authenticated browser session on `compute.arjun.cloud` created artifact `d45ca135-77af-470a-9662-35b0d6fd2443` for Qdrant point `58138b79-325b-4811-b589-c5a5e1415e82` under workspace `platform` through the new dashboard ingestion flow.
- Moved `arjnet-ui` off the public webhook for its own server-side gateway calls by adding `ARJNET_INTERNAL_GATEWAY_URL` to the OCI stack definition with default `http://ai-stack-gateway:8081/execute`, updating `_call_gateway()` to prefer that internal route, syncing the updated `arjnet-ui.py` plus `docker-compose.yml` to OCI, and recreating `arjnet-ui` so the new env was present inside the live container.
- Tightened the approval and guardrail layer in `oci-ai-stack/arjnet-ui.py`: `POST /api/v1/control/approvals` now requires an Authentik-authenticated identity that is already mapped into an active membership on the target workspace, and `approved` or `rejected` decisions now additionally require one of the workspace roles `owner`, `admin`, or `approver`. The endpoint also stops trusting caller-supplied audit names and instead stamps `requested_by` / `approved_by` from the live browser session identity while storing the acting principal id and role in approval metadata.
- Validated the new control-plane behavior live after the rollout: the recreated OCI `arjnet-ui` container reported `ARJNET_INTERNAL_GATEWAY_URL=http://ai-stack-gateway:8081/execute`, an authenticated browser session on `compute.arjun.cloud` still ingested a new knowledge artifact (`17585382-2f70-4280-9afb-eff84dc310c9`) through `/api/v1/ingest`, a direct unauthenticated `POST` to the private `http://100.101.107.42:8083/api/v1/control/approvals` route returned `403`, and the authenticated browser session successfully created both a `pending` and an `approved` approval record with `requested_by` / `approved_by` set to `mrdioxide46@gmail.com` and metadata showing the acting `admin` membership.
- Extended the Authentik workspace-role model from approvals into the rest of the ArjNet control plane in `oci-ai-stack/arjnet-ui.py`: creating a workspace now requires an authenticated browser session and automatically grants the current session principal an `owner` membership, identity upserts are restricted to platform admins, membership writes require `owner` or `admin` on the target workspace, prompt/agent/tool/job/artifact writes require one of `owner`, `admin`, or `editor`, and `/api/v1/ingest` now enforces the same workspace-scoped write policy before recording a knowledge artifact.
- Tightened the session-mapping path itself so `POST /api/v1/control/authentik/session` is no longer a free self-assignment endpoint for any authenticated browser. The live session can still activate or refresh an already-mapped membership, but it can no longer attach itself to a populated workspace without an existing mapping; only the first binding on an empty workspace remains self-service.
- Rolled that broader control-plane authorization step live by syncing the updated `arjnet-ui.py` to `/home/ubuntu/services/oci-ai-stack/`, creating a timestamped backup of the previous live file first, and explicitly restarting `arjnet-ui` so the new guardrails and workspace-creator behavior were loaded.
- Validated the unified browser workflow after the rollout with a fresh workspace `workspace-unified-1780157059486`: the authenticated browser session created the workspace with auto-owner email `mrdioxide46@gmail.com`, observed the creator membership as `owner`, added an invited viewer membership, registered a prompt, agent, tool binding, job ledger row, manual artifact, ingested a knowledge document that created Qdrant-backed artifact `4fc1a345-0267-4806-9e30-a2cdd66c8ff8`, and created an approved guardrail record that stamped `requested_by` and `approved_by` from the live session instead of the spoofed request body values.
- Validated the negative path on the private OCI app port as well: unauthenticated direct writes to `/api/v1/control/workspaces`, `/api/v1/control/memberships`, `/api/v1/control/prompts`, and `/api/v1/ingest` now return `403` instead of mutating the control plane outside the Authentik workspace model.
- Extended the same Authentik workspace model to control-plane reads in `oci-ai-stack/arjnet-ui.py`: `GET /api/v1/control/overview`, `workspaces`, `identities`, `memberships`, `prompts`, `agents`, `tools`, `jobs`, `artifacts`, and `approvals` now derive scope from the authenticated session and only expose workspaces where the caller has an active membership, with an optional `workspace_slug` further narrowing the result set.
- Split session binding into two explicit flows for the live dashboard and API: `POST /api/v1/control/authentik/session/accept` now activates an invited membership for the current Authentik principal, `POST /api/v1/control/authentik/session/assign` now handles admin-controlled current-session mapping, and the embedded dashboard session card plus the legacy `POST /api/v1/control/authentik/session` path were updated to reflect those separate semantics.
- Added an active workspace selector to the `compute.arjun.cloud` control-plane dashboard so the unified web app can drive session-aware reads instead of loading every accessible registry globally. The page now reloads prompts, memberships, agents, jobs, artifacts, and approvals for the selected workspace, syncs that workspace into the form inputs, and auto-targets pending invite acceptance to a real invited workspace instead of leaving the placeholder `platform` value in place.
- Rolled the scoped-read and session-flow update live by backing up `/home/ubuntu/services/oci-ai-stack/arjnet-ui.py`, syncing the updated repo file over SSH with the OCI key, and explicitly restarting `arjnet-ui` with `docker compose restart arjnet-ui` so the bind-mounted Python changes were loaded in the running container.
- Validated the live browser workflow after the rollout with disposable workspaces `workspace-scope-1780157739556`, `workspace-accept-1780157739556`, and `workspace-assign-1780157739556`: the authenticated Authentik session created each workspace, turned the accept workspace membership into `viewer/invited`, activated it through `POST /api/v1/control/authentik/session/accept`, changed the assign workspace current-session role to `admin` through `POST /api/v1/control/authentik/session/assign`, and then confirmed the dashboard workspace selector showed only `Assign Prompt 1780157739556` while hiding the prompt from the scope workspace when `workspace-assign-1780157739556` was active.
- Validated the new negative read path on the private OCI app port too: direct unauthenticated `GET` requests to `http://100.101.107.42:8083/api/v1/control/overview` and `http://100.101.107.42:8083/api/v1/control/prompts?limit=8` now return `403` with `Authentik authentication is required for this action`, proving the control-plane registry no longer serves anonymous reads on the private port.
- Rebuilt the ArjNet dashboard UX into a modern, responsive unified operations console in `oci-ai-stack/arjnet-ui.py`: updated visual system (typography, spacing, responsive layout, elevated cards, and action rail), added an operator hero section with one-click refresh controls, and preserved the existing compute plus control-plane workflows under the new design shell.
- Added a new authenticated stack inventory and management API route, `GET /api/v1/platform/apps`, backed by a curated catalog of edge, identity, control-plane, automation, LLM, retrieval, storage, devtool, monitoring, and MCP app surfaces; optional `?check=true` now performs server-side health probes with status code and latency capture for the UI.
- Added a new `Platform and Stack Management` section to `https://compute.arjun.cloud/` with stack filters, app cards, open-app/open-health actions, inventory summary, and on-demand full-stack health checks so operators can manage all major services from the same surface as registry and compute controls.
- Rolled the UX and platform-management update live by backing up `/home/ubuntu/services/oci-ai-stack/arjnet-ui.py`, syncing the updated repo file to OCI over SSH with the existing key, and explicitly restarting `arjnet-ui` using `docker compose restart arjnet-ui`.
- Validated the live result in the authenticated browser after reload: the new `Unified Stack Operations Console` hero and `Platform and Stack Management` section rendered, stack chips populated across categories, and `loadPlatformApps(true)` reported `11 apps tracked` with live health status and latency values in the app cards.
- Extended ArjNet with a non-AI control-plane primitive in `oci-ai-stack/arjnet-ui.py`: added a workspace-scoped `ops_tasks` registry table plus indexes, backed by new endpoints `GET /api/v1/control/ops/tasks`, `POST /api/v1/control/ops/tasks`, and `PATCH /api/v1/control/ops/tasks/{task_id}` for task tracking, ownership, status transitions, and operational notes.
- Added an `Ops Task Tracker` card to the unified dashboard so infrastructure work can be managed in the same workspace-scoped surface as approvals and jobs. The card supports task creation (title, type, priority, owner, due target, details), inline status transitions (`in_progress`, `blocked`, `done`), and scoped list rendering through the active workspace selector.
- Rolled the non-AI ops-task update live by backing up `/home/ubuntu/services/oci-ai-stack/arjnet-ui.py`, syncing the updated file to OCI, and restarting `arjnet-ui` via `docker compose restart arjnet-ui`.
- Validated the feature end to end from an authenticated browser session: created task `Ops smoke task 1780159174844` through `POST /api/v1/control/ops/tasks`, updated the same task to `done` through `PATCH /api/v1/control/ops/tasks/6ecbbb51-3a27-4b4a-b962-b2d88a0d7a25`, and confirmed scoped listing through `GET /api/v1/control/ops/tasks?limit=5&workspace_slug=platform` returned the updated task row.

## 2026-05-29

- Prepared repo-side Homepage compute cockpit groundwork for ArjNet by adding a dedicated `Compute` tab with a Modal-first compute-fabric section, future-provider cards for Kaggle and Colab, and action bookmarks for Modal, n8n, Cloudreve, Forgejo, and LiteLLM.
- Captured the intended external-compute operating model directly in the dashboard config: keep Modal as the primary burst layer, route orchestration through ArjNet n8n, hand off payloads and outputs through Cloudreve and the repo, and keep model routing plus budget policy on ArjNet through LiteLLM.
- Extended `oci-ai-stack/n8n-ai-stack-gateway.js` into a provider-aware compute contract by adding `describe_compute_fabric` and `prepare_compute_job` actions, a Modal/Kaggle/Colab registry, provider routing guidance, and normalized request payload planning for a Modal-first submit/poll workflow.
- Added `oci-ai-stack/modal-compute-fabric.py` as the first external worker entrypoint for ArjNet: a Modal app with authenticated `/submit` and `/result/{call_id}` endpoints plus initial `document_preprocess`, `chunk_text`, and `summarize_text` job types so n8n can offload preprocessing before OCI ingestion.
- Deployed the updated Homepage `settings.yaml`, `services.yaml`, and `bookmarks.yaml` to the live Hetzner Coolify mount at `/data/coolify/services/b12nv13lwujoev463rbiasdi/config/`, created timestamped backups of the previous files, restarted `homepage-b12nv13lwujoev463rbiasdi`, and verified the new `Compute` tab markers were present while the container returned to `healthy`.
- Synced `n8n-ai-stack-gateway.js` and `modal-compute-fabric.py` into the live OCI stack directory at `/home/ubuntu/services/oci-ai-stack/`, backed up the previous gateway file, and validated the server-side gateway by running `describe_compute_fabric` on OCI plus `python3 -m py_compile` for the Modal worker file.
- Left the external Modal endpoint intentionally un-deployed for now; the live gateway still reports Modal as `awaiting_endpoint` until `modal deploy` is run and `ARJNET_MODAL_COMPUTE_BASE_URL` is configured on the ArjNet side.
- Reworked the compute control plane into a Tailscale-brokered club model instead of pretending external notebook runtimes are direct tailnet members: the live gateway now reports `arjnet-club-compute`, explicit provider priority across Modal, Kaggle, and Colab, and a `tailscale_broker` section that keeps only the Hetzner edge and OCI control plane as direct tailnet nodes.
- Deployed the updated compute-mesh Homepage config live on Hetzner and verified new markers for `ArjNet Compute Mesh`, `Clubbed Compute Pool`, and `Tailscale Broker`, while the Homepage container again returned to `healthy` after restart.
- Synced the updated `n8n-ai-stack-gateway.js` and `.env.example` to the OCI stack so the live repo-side contract now documents `ARJNET_COMPUTE_POOL_NAME`, `ARJNET_COMPUTE_NETWORK_MODE=tailscale-brokered`, the Modal base URL placeholder, and the n8n callback URL placeholder for the combined compute environment.
- Installed and authenticated the local Modal CLI in the workspace `.venv`, which created the active Modal profile `ytview6`, then created the live secret `arjnet-modal-compute-auth` and synchronized the same `ARJNET_COMPUTE_TOKEN` into `/home/ubuntu/services/oci-ai-stack/.env` on OCI without printing the token value.
- Deployed `oci-ai-stack/modal-compute-fabric.py` to Modal at `https://ytview6--arjnet-compute-fabric-modal-compute-api.modal.run`, updated the live OCI `.env` with `ARJNET_MODAL_COMPUTE_BASE_URL`, `ARJNET_COMPUTE_RESULT_CALLBACK_URL`, `ARJNET_COMPUTE_POOL_NAME`, and `ARJNET_COMPUTE_NETWORK_MODE`, then validated `/health`, authenticated `/submit`, and authenticated `/result/{call_id}` from OCI.
- Fixed the first live Modal route bug in `modal-compute-fabric.py`: the original `/submit` handler shape kept returning `422` because the FastAPI body binding was too implicit for this nested ASGI pattern. The live fix was to parse the JSON body explicitly, then force a `modal deploy --strategy recreate` so the stale web endpoint signature was fully replaced.
- Revalidated the live OCI gateway after Modal setup by sourcing the updated `.env` and running both `describe_compute_fabric` and `prepare_compute_job`; Modal now reports `integration_status=configured`, a real `submit_url`, a real `poll_url_template`, and `ready_to_submit=true` for the clubbed compute pool.
- Replaced the first n8n compute-orchestration attempt after discovering that this live n8n 2.18.5 build cannot activate workflows containing `n8n-nodes-base.executeCommand`. The durable fix was to run `n8n-ai-stack-gateway.js` as an internal OCI service (`ai-stack-gateway`) over HTTP and rewrite `n8n-ai-stack-ping.json` to use only supported `httpRequest`, `function`, and `if` nodes.
- Deployed the internal `ai-stack-gateway` service on OCI, verified `http://ai-stack-gateway:8081/health` from inside the n8n container, then re-imported and re-published the `ai-stack-ping-workflow` export against the live n8n instance.
- Repaired the production webhook route again by reinserting the `webhook_entity` row for `ai-stack-ping-workflow` with path `ai-stack-ping-workflow/webhook/ai-stack-ping`, then pruned the stale workflow-history import that still contained `executeCommand`; after that cleanup, `n8n` finally activated `AI Stack Gateway` cleanly on startup.
- Verified the public AI Stack Gateway route end to end after the refactor: `ping` returns `200`, `describe_compute_fabric` returns the brokered Modal/Kaggle/Colab control-plane contract, `run_compute_job` submits a real Modal job and returns `pending` plus a `poll_request`, and `get_compute_job_result` on that returned payload completes successfully with the Modal summary result.
- Extended the repo-side Modal worker with a first training-oriented job type, `train_text_classifier`, using a scikit-learn TF-IDF plus logistic-regression pipeline and persisted artifact output under the Modal volume name `arjnet-compute-artifacts`; this groundwork is in the repo and locally validated, but not yet synced to OCI or redeployed to the live Modal endpoint.
- Updated the repo-side gateway and public `ai-stack-ping` workflow so training submissions can carry `records`, `dataset`, `training_dataset`, `training_config`, `model_name`, and `artifact_path` through to Modal instead of being dropped by the n8n normalization step.
- Validated the new training slice locally by running `python -m py_compile` on `modal-compute-fabric.py`, `node --check` on `n8n-ai-stack-gateway.js`, an inline `train_text_classifier` execution that produced artifacts in a temp directory, and Node-based checks that the workflow normalization plus `prepareComputeJob` both build a submit-ready Modal payload.
- Synced the updated `n8n-ai-stack-gateway.js`, `n8n-ai-stack-ping.json`, and `modal-compute-fabric.py` into `/home/ubuntu/services/oci-ai-stack/`, redeployed the Modal app with `modal deploy --strategy recreate`, and republished `ai-stack-ping-workflow` in the live n8n instance.
- Root-caused the first live training failure to a stale `ai-stack-gateway` Node process: the bind-mounted gateway file had updated on OCI, but `docker compose up -d ai-stack-gateway n8n` did not recreate the already-running gateway container, so it kept serving the old in-memory code until `docker restart oci-ai-stack-ai-stack-gateway-1`.
- Revalidated the live public compute route with a real `run_compute_job` request for `train_text_classifier`; after four rapid public polls, the workflow completed successfully and returned the trained-model artifact paths under `/artifacts/models/live-smoke-test` plus the expected metrics payload.
- Connected Kaggle and Colab into ArjNet as brokered notebook providers instead of pretending they were automated workers: `run_compute_job` for Kaggle now returns a `handoff_prepared` payload for `dataset_pipeline` and related notebook work, `run_compute_job` for Colab returns a similar interactive-session handoff, and both include a notebook bootstrap that calls back into ArjNet through `register_external_job_result`.
- Added `register_external_job_result` to the live gateway and public `ai-stack-ping` workflow so Kaggle and Colab notebooks can POST completed summaries, metrics, and artifact references back into ArjNet, optionally receiving an `ingest_request` payload for indexing notebook reports through the existing `ingest_document` path.
- Fixed the first live Kaggle/Colab callback bug by changing the default notebook completion webhook from the Modal-oriented `ARJNET_COMPUTE_RESULT_CALLBACK_URL` value to the public AI Stack Gateway route `https://n8n.arjun.cloud/webhook/ai-stack-ping-workflow/webhook/ai-stack-ping`; after restarting `ai-stack-gateway`, live public validation succeeded for Kaggle handoff, Colab handoff, and Kaggle result registration.
- Verified that the supplied Kaggle API token can authenticate without a separate username and can reveal the owner slug from the authenticated account. Based on that probe, deployed a new internal OCI service `kaggle-notebook-broker` with authenticated `/submit` and `/result/{call_id}` endpoints, then wired `n8n-ai-stack-gateway.js` so Kaggle becomes an opt-in automated provider when `job_config.auto_submit=true`.
- Synced `kaggle-notebook-broker.py`, `kaggle-notebook-broker.Dockerfile`, `n8n-ai-stack-gateway.js`, and `docker-compose.yml` to `/home/ubuntu/services/oci-ai-stack/`, updated the live OCI `.env` with `ARJNET_KAGGLE_BROKER_URL`, `ARJNET_KAGGLE_OWNER`, `ARJNET_PUBLIC_COMPUTE_WEBHOOK_URL`, and `KAGGLE_API_TOKEN`, then rebuilt `kaggle-notebook-broker` and recreated `ai-stack-gateway`.
- Validated the new live Kaggle automation path end to end through the public `ai-stack-ping` webhook: `describe_compute_fabric` now reports Kaggle as `integration_status=configured`, a real `submit_url`, and a real `poll_url_template`; a public `submit_compute_job` request with `provider=kaggle`, `job_type=dataset_pipeline`, and `job_config.auto_submit=true` returned `submitted`; and a follow-up `get_compute_job_result` poll returned `pending` with the Kaggle notebook viewer URL.
- Hardened the first Kaggle poll implementation after live testing: Kaggle may refuse direct status reads for private notebooks even on the owning account, so the broker now keeps notebook titles aligned with their kernel slugs and degrades unreadable private-status checks to `pending` instead of crashing the public workflow. The completion path still relies on the notebook callback into `register_external_job_result`.
- Extended the public Colab handoff payload with a `terminal_bootstrap` string in addition to the existing notebook bootstrap so a signed-in Colab terminal session can POST results back to ArjNet without manual payload assembly. Live public validation confirmed `submit_compute_job` for `provider=colab` returns `handoff_prepared` with both bootstrap forms.
- Created `docs/` with a README overview, per-provider guides (`modal.md`, `kaggle.md`, `colab.md`), and a live examples document (`examples.md`) containing real run outputs from all three providers. Every example was executed against the live public endpoint on 2026-05-29:
  - **Modal document_preprocess:** submitted, polled once, returned cleaned text with stats in under 3 seconds.
  - **Modal train_text_classifier:** 10 records across 2 labels, trained instantly, returned accuracy metrics and artifact paths.
  - **Kaggle automated dataset_pipeline:** submitted via `auto_submit=true`, returned `call_id` and `viewer_url`, poll returned `pending` with the live notebook viewer link.
  - **Kaggle manual model_eval:** returned `handoff_prepared` with launch URL, terminal bootstrap, notebook bootstrap, and dataset refs.
  - **Colab interactive_session:** returned `handoff_prepared` with `terminal_bootstrap`, `notebook_bootstrap`, completion webhook, and next steps.
- Built and deployed HF training to Modal with transformers 4.48.0, sentencepiece, tiktoken, evaluate, datasets, accelerate, torch, and T4 GPU support. After debugging 5.x → 4.48 API differences (eval_strategy, warmup_ratio, tokenizer parameter removal, remove_unused_columns=False), the first OT security classifier trained successfully in 36 seconds on 8 ICS security examples with 4 classes, returning accuracy 0.5 and F1 0.333. Full SecureBERT, RoBERTa, BERT-tiny, and 6 other models available via training_config.base_model.
  - Publicly accessible at `https://compute.arjun.cloud` with Swagger at `/docs` and ReDoc at `/redoc`.
  - Added the `compute.arjun.cloud` route to both Hetzner Traefik config files (`oci-ai-stack.yaml` and `arjun-authentik-sso.yaml`) without Authentik SSO.
  - JS syntax was fixed after discovering a literal newline in a `split('\n')` call inside the inline dashboard script; Node.js syntax validation confirmed clean after the fix.

## 2026-05-28
- Reconciled the current repo state into a single timeline covering both servers, the public edge, and the private OCI workloads.
- Reworked the Homepage configuration into an ArjNet-branded dashboard layout.
- Moved the update workflow and OCI runtime version inventory into visible Homepage service sections instead of relying mainly on bookmarks.
- Added explicit Homepage server-health cards for the Hetzner and OCI hosts using Netdata widgets plus direct host health checks over Tailscale.
- Deployed the updated Homepage YAML files to the live Hetzner mount at `/data/coolify/services/b12nv13lwujoev463rbiasdi/config/`, created timestamped backups of the previous files, and restarted `homepage-b12nv13lwujoev463rbiasdi` to apply the changes.
- Installed `glances` on both servers and created a `glances-web.service` bound to each host's Tailscale IP on port `61208` so Homepage can display richer host CPU, memory, disk, and uptime metrics.
- Updated live Homepage `widgets.yaml` on Hetzner to consume both Glances endpoints and verified connectivity from inside `homepage-b12nv13lwujoev463rbiasdi` to `http://100.64.75.39:61208/api/3/status` and `http://100.101.107.42:61208/api/3/status`.
- Replaced the Homepage server-health cards with Glances power-user entry points and added explicit `HG` and `OG` bookmark buttons that open the full Glances web UI for the Hetzner and OCI hosts.
- Added Authentik-protected public Glances routes under `dashboard.arjun.cloud/glances/hetzner/` and `/glances/oci/` via Hetzner Traefik strip-prefix middlewares, then repointed the Homepage power-user buttons to those public paths so they no longer depend on direct browser access to the Tailscale IPs.
- Added a dedicated `Glances` tab inside Homepage with embedded iframe consoles for both hosts, and repointed the overview Glances host cards to `dashboard.arjun.cloud/#glances` so clicking them opens the separate in-dashboard power page instead of the raw host page.
- Reworked the Glances UX after testing: Authentik login responses send `X-Frame-Options: DENY`, which can yield white iframe pages. The final Homepage behavior now points the overview Glances widgets and the Glances tab actions directly to the standalone full-page routes under `dashboard.arjun.cloud/glances/hetzner/` and `/glances/oci/` instead of embedding them.
- Root-caused the remaining blank standalone Glances pages to the Ubuntu/Debian `glances` package itself: it served an HTML shell that referenced `glances.js`, but the compiled web bundle was not present in the packaged static assets.
- Replaced the distro-backed `glances-web.service` on both hosts with an isolated `/opt/glances-web` virtualenv install of `glances[web]`, keeping each service bound to its Tailscale IP on port `61208` while switching `ExecStart` to the virtualenv binary.
- Validated the final repair on both servers by confirming `http://100.64.75.39:61208/static/glances.js` and `http://100.101.107.42:61208/static/glances.js` return `200 OK`, and then verified that the Authentik-protected public routes at `dashboard.arjun.cloud/glances/hetzner/` and `/glances/oci/` render the live Glances UI instead of a blank page.
- Fixed the remaining Glances frontend console fault on both hosts by installing the optional `glances[containers]` extra into each `/opt/glances-web` virtualenv, then restarting `glances-web.service` so the containers plugin returns a list of containers instead of an invalid `{}` object.
- Revalidated the Glances power-user pages after the containers fix by confirming `/api/4/containers` returns live Docker container arrays on both hosts and by reloading the public dashboard routes until both pages rendered without the previous containers-panel JavaScript error.
- Hardened both Glances web services in `/etc/glances/glances.conf` by setting explicit `webui_allowed_hosts` values for `dashboard.arjun.cloud`, localhost, and each host's direct Tailscale address, and by narrowing `cors_origins` to `https://dashboard.arjun.cloud` with non-credentialed CORS.
- Validated the Glances hardening on both hosts by confirming expected hosts still return `200`, an invalid `Host` header now returns `400 Bad Request`, only `https://dashboard.arjun.cloud` is echoed as `Access-Control-Allow-Origin`, and the previous Glances startup warnings about permissive CORS and missing host validation no longer appear in the service logs.
- Audited live disk usage on both servers after a Hetzner capacity concern. The check showed Hetzner still had free space but most of its local usage was platform overhead under `/var/lib/containerd`, `/var/lib/docker`, `/opt/netdata`, and logs, while OCI retained significantly more free capacity for additional workloads.
- Moved Uptime Kuma from Hetzner to OCI as the lowest-risk first offload: copied the `t139om2w5wie761wadsvyzum_uptime-kuma-data` volume to `/home/ubuntu/services/uptime-kuma/data`, deployed a new OCI-side compose stack bound to `100.101.107.42:3001`, and repointed `status.arjun.cloud` through Hetzner Traefik file-provider routing to the OCI service.
- Retired the old Hetzner Uptime Kuma container after OCI validation, removed the old `louislam/uptime-kuma:2` image from Hetzner, and increased Hetzner free disk from roughly `21G` to `24G` while keeping the original Docker volume in place as a rollback backup.
- Moved Forgejo from the downed Hetzner Coolify service to OCI by copying the live app data, exporting the Hetzner PostgreSQL database with a logical `pg_dump`, restoring it into a fresh OCI PostgreSQL container, and bringing up a new OCI compose stack at `/home/ubuntu/services/forgejo` bound to `100.101.107.42:3002` for HTTP and `100.101.107.42:22222` for SSH.
- Repointed `git.arjun.cloud` on the Hetzner Traefik file-provider to the OCI Forgejo service, validated the public UI in the browser, and kept the old host-side SSH entrypoint in place with a `forgejo-ssh-forward.socket` and `forgejo-ssh-forward.service` pair that proxies local `22222` traffic from Hetzner to OCI over Tailscale.
- Removed the no-longer-needed Hetzner Forgejo image and the Hetzner Netdata install under `/opt/netdata`, stopped the lingering Netdata namespace units, and increased Hetzner free disk again to roughly `27G` while leaving the old Forgejo Docker volume in place as rollback data.
- Deployed the live Homepage cleanup so the Hetzner deep-view buttons now open Glances instead of the retired Hetzner Netdata UI, and updated the public app inventory notes to reflect that Uptime Kuma and Forgejo now run on OCI behind the Hetzner edge.
- Opened the upstream Hetzner firewall path for Forgejo SSH on `49.13.3.97:22222` and validated external SSH handshakes to the Hetzner entrypoint from both OCI and the local Windows workstation while the host-local socket proxy continued relaying to OCI Forgejo over Tailscale.
- Added the current workstation's `id_ed25519` public key to the Forgejo `ar606` account and verified `ssh -T -p 22222 git@49.13.3.97` authenticates successfully to Forgejo with the `NBLemgo2-id_ed25519` key, returning the expected no-shell-access banner.

## 2026-05-16

- Updated Hetzner Traefik and Authentik integration for the public edge.
- Added or verified Authentik SSO protection for `dashboard.arjun.cloud`, `ai.arjun.cloud`, `n8n.arjun.cloud`, `llm.arjun.cloud`, `letta.arjun.cloud`, `search.arjun.cloud`, `drive.arjun.cloud`, `status.arjun.cloud`, and `mcp.arjun.cloud`.
- Kept the required public exceptions in place:
  - `n8n` webhook and form paths remain public for automation.
  - LiteLLM `/v1` remains reachable for API-key clients.
  - Cloudreve API, share, and static asset paths remain reachable while the main UI stays SSO-gated.
- Simplified Letta browser access by injecting the bearer authorization header at Traefik instead of relying on manual browser entry.
- Confirmed the Hetzner host is acting as the public edge and access-control layer for both local services and OCI-published services.

## 2026-05-03

- Built out the OCI private AI stack in `oci-ai-stack/docker-compose.yml`.
- Bound the OCI services to the Tailscale interface instead of exposing them directly on public ports.
- Defined the OCI workloads: Postgres with `pgvector`, Ollama, LiteLLM, Qdrant, SearXNG, Open WebUI, MCPO, MCPO Python, Letta, and n8n.
- Added Hetzner Traefik routing for the OCI-published apps: `ai.arjun.cloud`, `n8n.arjun.cloud`, `llm.arjun.cloud`, `letta.arjun.cloud`, `search.arjun.cloud`, `drive.arjun.cloud`, and `mcp.arjun.cloud`.
- Configured LiteLLM with a database-backed admin UI, explicit UI credentials, `STORE_MODEL_IN_DB=true`, and `PROXY_BASE_URL=https://llm.arjun.cloud`.
- Bulk-synced the OpenRouter text-model catalog into LiteLLM and verified the public `/v1/models` surface.
- Wired Open WebUI to Ollama, LiteLLM, Qdrant, SearXNG, and both MCPO gateways.
- Configured MCPO as two OCI-side tool gateways:
  - Docker tools via `uvx mcp-server-docker`
  - Python and Jupyter tools via `uvx mcp-server-jupyter`
- Connected Letta to Postgres and the model backends and exposed it through the Hetzner edge.
- Established the cross-server pattern used throughout the stack: Cloudflare to Hetzner Traefik to OCI over Tailscale.

## 2026-05-02

- Created the Homepage dashboard configuration for the operations view.
- Added dashboard settings, widgets, bookmarks, and service entries for the platform, AI stack, development tools, and infrastructure links.
- Used public URLs for user navigation and OCI Tailscale `siteMonitor` endpoints for reliable health checks from the Hetzner edge.
- Added infrastructure visibility for both servers through Netdata links over Tailscale.
- Added the n8n probe workflow files used to verify and exercise the AI stack gateway.

## Current Server Roles

- Hetzner server: public edge, Traefik routing, Authentik SSO, Coolify-facing control plane, dashboard access, and shared operational entry point.
- OCI server: private workload host for the AI stack plus the offloaded Uptime Kuma and Forgejo services, published through Hetzner over Tailscale.
- Cross-server pattern: OCI stays private; Hetzner owns the public TLS entry points and forwards approved traffic to OCI services.

## Source Evidence

- `hetzner-traefik/arjun-authentik-sso.yaml`
- `hetzner-traefik/oci-ai-stack.yaml`
- `oci-ai-stack/docker-compose.yml`
- `oci-forgejo/docker-compose.yml`
- `oci-uptime-kuma/docker-compose.yml`
- `oci-ai-stack/mcpo/config.json`
- `oci-ai-stack/mcpo/config-python.json`
- `oci-ai-stack/n8n-ai-stack-ping.json`
- `oci-ai-stack/n8n-fetch-probe.json`
- `homepage-dashboard/config/services.yaml`
- `homepage-dashboard/config/settings.yaml`
- `homepage-dashboard/config/widgets.yaml`
- `homepage-dashboard/config/bookmarks.yaml`